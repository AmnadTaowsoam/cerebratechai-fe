version: '3.8'

services:
  # Frontend Web Application
  web:
    image: alpine:3.18
    command: ["sh", "-c", "echo 'cerebratechai web prod disabled (placeholder)'; sleep infinity"]
    container_name: cerebratechai-web-prod
    ports:
      - '3220:3000'
    restart: unless-stopped
    networks:
      - cerebratechai-network

networks:
  cerebratechai-network:
    driver: bridge
